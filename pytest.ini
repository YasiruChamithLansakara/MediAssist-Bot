[pytest]
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning

addopts = -q --cov=app --cov-report=html
asyncio_mode = auto
markers =
    unit
    integration
    rag
    ocr