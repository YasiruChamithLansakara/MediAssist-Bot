# LLM dependencies for MediAssist chat features
# Install: pip install -r requirements-llm.txt
#
# Primary: Groq API (FREE — Llama 3.1 8B Instant)
# Sign up at https://console.groq.com
groq>=0.9.0

# Fallback: OpenAI (paid)
openai>=1.0.0

# HTTP client (used by all providers as fallback)
requests>=2.28.0
