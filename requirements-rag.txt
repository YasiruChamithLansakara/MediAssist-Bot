# Optional dependencies for RAG Vector Search
# Install with: pip install -r requirements-rag.txt

# For OpenAI embeddings (recommended)
openai>=0.27.0

# For local embeddings (sentence-transformers)
# Lighter weight than OpenAI, works offline
sentence-transformers>=2.2.0

# For TF-IDF fallback embeddings
scikit-learn>=1.0.0

# For enhanced performance
numpy>=1.20.0

# Alternative embedding providers (optional)
# anthropic>=0.7.0  # For Claude embeddings
# cohere>=3.0.0     # For Cohere embeddings

# Vector similarity computation (included with numpy, but can optimize with)
# faiss-cpu>=1.7.0  # For CPU-based similarity search (more efficient for large datasets)
