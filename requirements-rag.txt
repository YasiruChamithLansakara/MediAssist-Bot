# RAG / Vector Search dependencies for MediAssist
# Install: pip install -r requirements-rag.txt
#
# Semantic embeddings — LOCAL, FREE, no API key needed (RECOMMENDED)
sentence-transformers>=2.6.0

# FAISS vector index (CPU)
faiss-cpu>=1.7.4

# Numeric / ML utilities
numpy>=1.24.0
scikit-learn>=1.3.0   # TF-IDF fallback + data processing

# Optional: OpenAI embeddings (paid, higher quality)
# openai>=1.0.0
